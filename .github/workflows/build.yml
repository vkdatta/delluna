name: Delluna Build

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm test
      - run: npm run build
      - run: npm run validate
      - name: Commit generated distribution
        run: |
          git config user.name "Delluna Builder"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add registry dist
          git diff --cached --quiet || git commit -m "build: update Delluna generated artifacts [skip ci]"
          git push
